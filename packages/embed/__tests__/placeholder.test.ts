import { describe, it, expect } from "vitest"

describe("embed package", () => {
  it("placeholder — embed widget tests will be added when widget code is implemented", () => {
    expect(true).toBe(true)
  })
})
